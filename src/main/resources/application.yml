spring:
  application:
    name: meetime-case-tecnico

  profiles:
    active: ${ENVIRONMENT}

auth:
  hubspot:
    url: ${HUBSPOT_AUTH_URL}
    apikey: ${HUBSPOT_API_KEY}

openapi:
  server:
    local: ${LOCAL_URL}
    prd: ${PROD_URL}